#day2
#胡言胡语：
 无敌java，到底要ai辅助还是老老实实打好基础，感觉不自己一个人来码，一点安全感都没有，感觉写写中文然后按tab按到死就可以了，wq，代码余也太简单了吧（doge）
 1.字面量
  	数字类
<<<<<<< HEAD
		整数（int,byte,long(数据后要有L),short）：直接写
		小数（double,float（数据后要有F））：直接写，加小数点
	文本类
		字符串（spring）：用双引号
		字符（char）（单个字or字母）：可以双引号or单引号
	其他
		布尔类型（boolean）：true，false
=======
		整数（int）：直接写
		小数（double）：直接写，加小数点
	文本类
		字符串：用双引号
		字符（单个字or字母）：可以双引号or单引号
	其他
		布尔类型：true，false
>>>>>>> 41c0e08bd26f0d65a11ae8c88141a1405d430de1
		空类型： null
 2.变量
   使用场景：
	 重复使用某个值or =某个数据经常发生变化
   格式：
 	 数据类型 变量名 = 数据值;

   用途：1.定义变量，进行输出
		int a =10;
		System.out.println(a);
	 2.参与计算
		int b=10,c=20;
		System.out.println(b=c);
	 3.修改记录变量的值
		int a=10;
		a=50;
		System.out.println(a);//50
   注意事项：
	1.变量只能存储一个值
		错误：int a =10;
		      int a =20;
	2.变量名不能重复
	3.一条语句可以定义多个变量（见用途2）

#碎碎念：
  	总是忘记  每条代码后面一定要加上“;”
	写完代码可以看看每一个"{"有没有对应的"}"
	在IDEA上写的代码最一开始会有路径，导致在linux上运行的时候会报错，建议运行前可以把前面那一串删掉，经过检验，运行没有问题qwq

   
   
